import React    from "react";
import template from "./TaskFormulaire.jsx";

class TaskFormulaire extends React.Component {
  render() {
    return template.call(this);
  }
}

export default TaskFormulaire;
